# CHANGELOG

## V2.0.3（2026-08-27）

主题：结构工程化 + 依赖分发完整性 + 运行时节流。

- 新增：负向触发规则（第三章）——纯咨询、解释、概念问答直接回答，不触发读取链、不进入研发工作流、不沉淀记忆
- 新增：子技能调度去重（第九章）——同一任务同一子技能只加载一次
- 新增：记忆文件体量控制（第五章）——单文件超 200 行提醒整理，整理属档三不自动执行
- 新增：验证输出摘要规则（第十三章）——只提取结论与错误段落，完整日志不进上下文
- 收紧：`brainstorming` 仅大型新功能或方向不明的需求调用，中小型调整走档二内置单问
- 删除：dependencies 移除 `grilling` 冗余声明（由 `grill-me` 传递加载）
- 工程化：模板拆分 `src/` 章节源文件；SKILL.md 与版本模板改由 `scripts/build.mjs` 生成；验收由 `scripts/validate.mjs` 自动执行；新增 `AGENTS.md`
- 分发：`skills/` 目录携带全部依赖子技能副本，`scripts/install.mjs` 一键部署 dev-copilot 与依赖，支持分发给他人使用

## V2.0.2 及更早

V2.0.2 及更早为项目化前的单文件模板，各文件开头的版本定位章节记录了当版变更，存档于上层优化工作区，不在本仓库内。
