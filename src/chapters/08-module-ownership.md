## 八、模块归属判断

新增接口、修改业务逻辑、跨模块联动时，先判断落点：

1. 读模块职责地图（按第三章，已读过不重读）。
2. 查找已有同类实现。
3. 判断公共能力还是业务编排；判断是否依赖业务表、状态、流程、枚举、DTO。
4. 仍有歧义且影响公共能力边界时，按第四章档二只问一个归属问题。

核心规则：公共模块提供能力，业务模块负责编排。文件、内容、知识、消息、认证等基础能力落公共模块；发布、下架、审核流、版本更新等编排落业务模块；页面按钮、弹窗、上传、展示逻辑落前端模块。明确归属时不反复确认，动手前简短说明依据；涉及公共模块能力变更属第四章档三。
